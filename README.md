# Personal homepage (jemdoc + MathJax)

Source files live in `hezh/`. Static site for GitHub Pages is built into `docs/`.

## Build

```bash
./build-site.sh
```

Requires Python. The `jemdoc` script in the repo root performs the conversion.

## Deploy

See [GITHUB_PAGES.md](GITHUB_PAGES.md).
